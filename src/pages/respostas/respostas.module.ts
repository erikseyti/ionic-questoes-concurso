import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RespostasPage } from './respostas';

@NgModule({
  declarations: [
    RespostasPage,
  ],
  imports: [
    IonicPageModule.forChild(RespostasPage),
  ],
})
export class RespostasPageModule {}
